define ->

	# manifest array for site/modules/preloader
	# valid object contains `src` and `type` with
	# `attr` for declaring src property name

	class Assets

		manifest: [
			src: "models/json/balloon.js"
			type: "THREE"
		,
			src: "models/json/fan/base.js"
			type: "THREE"
		,
			src: "models/json/fan/blades.js"
			type: "THREE"
		]